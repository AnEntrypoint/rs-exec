[package]
name = "rs-exec"
version = "0.1.3"
edition = "2021"

[lib]
name = "rs_exec"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[features]
wasm = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
